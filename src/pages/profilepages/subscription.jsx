import React from "react";
import ProfileLayout from "@/components/shared/layout/profilelayout";

const Subscription = () => {
    
    return (
        <ProfileLayout page_title="Subscription">
           
        </ProfileLayout>
    );
};

export default Subscription;
